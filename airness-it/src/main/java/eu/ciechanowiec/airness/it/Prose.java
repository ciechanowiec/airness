package eu.ciechanowiec.airness.it;

/**
 * A fixture whose comment prose breaks both rules on purpose; that semicolon is the first of them,
 * where a full stop would have read shorter.
 */
final class Prose {

    /**
     * Reads the fixed value this fixture carries.
     *
     * @return the fixed value.
     */
    int value() {
        return 0;
    }
}
