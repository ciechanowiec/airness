#!/usr/bin/env sh
# The fast guard before a commit is recorded: everything the default build checks, which is every gate
# that needs neither the network nor the whole history. The slow half runs on push and in CI.
set -eu
cd "$(git rev-parse --show-toplevel)"
mvn -q clean package
