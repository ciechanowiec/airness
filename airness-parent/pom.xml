<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.ciechanowiec</groupId>
        <artifactId>airness</artifactId>
        <version>1.0.6</version>
    </parent>

    <artifactId>airness-parent</artifactId>
    <packaging>pom</packaging>

    <name>airness-parent</name>
    <description>
        The parent a consuming project inherits. It carries the plugin configuration and the lifecycle
        bindings, so a consumer states its coordinates and its dependencies and receives the harness.

        It binds the analyzers, tests, coverage, source normalization, and repository governance. A
        consumer updates the harness by raising the parent version and synchronizing the managed files.
    </description>

    <properties>
        <!--
            The phase at which each source-shaping tool applies its changes. Only the applying half is a
            property. The checking half is bound to a literal phase and cannot be moved by anyone.

            The asymmetry prevents a consumer from moving a checking phase to "none" in its own pom.
            Setting an applying phase can only add work: the tool applies its changes first and the
            check still runs afterwards, so the check confirms that the apply did its job.

            The format profile below is what sets them. Each applying execution is declared ahead of its
            checking one, so within the shared phase the apply runs first.
        -->
        <airness.source.formatting.apply>false</airness.source.formatting.apply>
        <!--
            Where the harness unpacks what it supplies. Under the build directory rather than in the
            tree, because a copy in the tree is a copy that gets edited and then ignored, and because
            the build directory is already ignored by git and so is invisible to the tree-mutation net.
        -->
        <!--
            The field annotations NullAway treats as initializing a field it would otherwise call
            uninitialized: a value Maven, JUnit, picocli, or an injector writes rather than a
            constructor. Named once because the report-only profile below has to restate the whole
            compiler argument to relax it, so the list would otherwise be hand-copied three times.
        -->
        <airness.nullaway.excluded-field-annotations>org.apache.maven.plugins.annotations.Parameter,org.apache.maven.plugins.annotations.Component,org.junit.jupiter.api.io.TempDir,picocli.CommandLine.Option,picocli.CommandLine.Parameters,javax.inject.Inject,jakarta.inject.Inject</airness.nullaway.excluded-field-annotations>
        <!--
            The package NullAway treats as annotated. Everything outside it is assumed unannotated and
            goes unchecked, so a value naming a package this project does not use turns the whole
            null-checker into a no-op that still appears in the build log. check-parameters refuses to
            let it stay unset.
        -->
        <airness.package.root>UNSET</airness.package.root>
        <!--
            The ceiling on one test, one lifecycle method, or one template invocation. Declared here
            rather than left to the aggregator this parent inherits, because this is the file a consumer
            inherits, and every other default the user guide documents is stated in it.
        -->
        <airness.test.timeout>30 s</airness.test.timeout>
        <airness.coverage.skip>${skipTests}</airness.coverage.skip>
        <!-- One switch controls whether findings fail the build. skipTests is the separate bypass. -->
        <airness.enforce>true</airness.enforce>
        <!--
            Classes left out of the per-class coverage floor, comma-separated, each because of the role
            it plays rather than because covering it was inconvenient. Empty by default: a harness that
            arrived with exclusions would be teaching a project that the list is where difficulty goes.
        -->
        <airness.coverage.excluded.classes></airness.coverage.excluded.classes>
        <skipTests>false</skipTests>
        <airness.rewrite.apply.phase>none</airness.rewrite.apply.phase>
    </properties>

    <!-- Versions come from the root dependency management. These declarations supply the dependencies. -->
    <dependencies>
        <dependency>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-annotations</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>
        <!--
            Inherited by every consumer, so the annotation the rule set expects is on the compile
            classpath without anyone declaring it. A rule that demands an annotation a project has to go
            and write for itself is a rule that gets worked around rather than followed.

            provided scope, because the annotation has SOURCE retention and is gone by the time the class
            file exists. Nothing needs it later, so nothing downstream should resolve it.
        -->
        <dependency>
            <groupId>eu.ciechanowiec</groupId>
            <artifactId>airness-annotations</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <configuration>
                        <!--
                            Dependency analysis reads class files, and a SOURCE-retention annotation
                            leaves none behind. airness-annotations is therefore used by every project
                            that suppresses a rule and reported as unused by all of them. The exemption is
                            named for this one artifact rather than switched off wholesale, so a genuinely
                            unused dependency is still reported.
                        -->
                        <ignoredUnusedDeclaredDependencies>
                            <ignoredUnusedDeclaredDependency>
                                eu.ciechanowiec:airness-annotations
                            </ignoredUnusedDeclaredDependency>
                        </ignoredUnusedDeclaredDependencies>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <!--
                    Error Prone is a javac plugin rather than a Maven one: it hooks the compiler's typed
                    AST while compiling. Since JDK 16 that compiler is strongly encapsulated, so reaching
                    it needs a forked javac carrying the exports and opens below.

                    NullAway rides along as an Error Prone check. It reads airness.package.root to decide
                    what counts as annotated code, and everything outside that package is assumed
                    unannotated and left alone, which is why a wrong value there disables the checker
                    while leaving it in the log looking configured.

                    The reflective-injection annotations are excluded from the field-initialization
                    check, because a field a framework assigns is not a field the constructor forgot.
                    The list is fixed rather than a property: a project that could extend it could also
                    empty it, and an empty list reads in the log exactly like a correct one.

                    doclint holds the Javadoc that Checkstyle only requires the presence of: a @param
                    naming an argument the method does not take, or a {@link} to a type that does not
                    resolve. The governance javadoc-links goal pushes every resolvable name into a link,
                    and without doclint nothing afterwards confirms the link it asked for reaches
                    anything. -missing is the one group left out, because Checkstyle's MissingJavadoc
                    rules read the source AST and so ask only for Javadoc on declarations that were
                    actually written, while doclint also demands it on the constructor Lombok's
                    @UtilityClass synthesizes.
                -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <fork>true</fork>
                    <compilerArgs>
                        <arg>-XDcompilePolicy=simple</arg>
                        <arg>--should-stop=ifError=FLOW</arg>
                        <arg>-Xplugin:ErrorProne -Xep:NullAway:ERROR -XepOpt:NullAway:AnnotatedPackages=${airness.package.root} -XepOpt:NullAway:ExcludedFieldAnnotations=${airness.nullaway.excluded-field-annotations}</arg>
                        <arg>-Werror</arg>
                        <arg>-Xlint:deprecation</arg>
                        <arg>-Xdoclint:all,-missing</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>com.google.errorprone</groupId>
                            <artifactId>error_prone_core</artifactId>
                            <version>${error-prone.version}</version>
                        </path>
                        <path>
                            <groupId>com.uber.nullaway</groupId>
                            <artifactId>nullaway</artifactId>
                            <version>${nullaway.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <!--
                    A timeout every test carries whether or not it declares one, so an unbounded wait
                    ends in a finding rather than in a build that hangs. Declared here rather than in a
                    junit-platform.properties in each project, because a file a project owns is a file a
                    project can empty.

                    JUnit's default thread mode is left in place: it reports a test that ran past the
                    ceiling rather than interrupting one mid-flight. Interrupting would also unstick a
                    true hang, at the cost of moving every test off the thread its fixtures were built
                    on, which is a large change to impose on a consumer to catch a case CI already bounds.
                -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <properties>
                        <configurationParameters>
                            junit.jupiter.execution.timeout.default = ${airness.test.timeout}
                            junit.jupiter.testclass.order.default = org.junit.jupiter.api.ClassOrderer$Random
                            junit.jupiter.testmethod.order.default = org.junit.jupiter.api.MethodOrderer$Random
                            junit.jupiter.execution.order.random.seed = 42
                        </configurationParameters>
                    </properties>
                </configuration>
            </plugin>
            <plugin>
                <!--
                    Coverage measured per class rather than over the module. A module-wide floor lets one
                    thoroughly covered class carry an untested one, and the arithmetic that makes that
                    work gets easier the larger the module grows, so the number stops meaning what it
                    says exactly when it matters most.

                    Both counters are held to the same floor. Instructions alone say a line ran, which a
                    single walk through a method achieves while leaving every branch in it untaken.
                -->
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <configuration>
                    <skip>${airness.coverage.skip}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>airness-coverage-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>airness-coverage-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>airness-coverage-check</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <haltOnFailure>${airness.enforce}</haltOnFailure>
                            <rules>
                                <rule>
                                    <element>CLASS</element>
                                    <excludes>${airness.coverage.excluded.classes}</excludes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <executions>
                    <execution>
                        <id>airness-enforce-dependencies</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <skip>${skipTests}</skip>
                            <fail>${airness.enforce}</fail>
                            <!--
                                The rules are not written here. They reach this execution from the
                                pluginManagement of the pom this one inherits, which is where they are
                                written once for a consumer and for the harness alike.
                            -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!--
                    Fails on a declared dependency nothing uses, and on a used one nothing declares, so
                    the declared set stays exactly what the code needs. Non-compile scopes are left out,
                    because this reads class files and cannot see a dependency used only through
                    annotations or reflection.
                -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <!--
                        Exposes each resolved dependency path as ${groupId:artifactId:type}. Java 25
                        modernization uses that standard property form when Mockito is already present
                        and must run as a test JVM agent instead of attaching itself dynamically.
                    -->
                    <execution>
                        <id>airness-dependency-properties</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>airness-analyze-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>analyze-only</goal>
                        </goals>
                        <configuration>
                            <skip>${skipTests}</skip>
                            <failOnWarning>${airness.enforce}</failOnWarning>
                            <ignoreNonCompile>true</ignoreNonCompile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!--
                    Holds every committable text file to the .editorconfig the harness owns. Binaries,
                    IDE metadata, and build output are exempt by role.
                -->
                <groupId>org.ec4j.maven</groupId>
                <artifactId>editorconfig-maven-plugin</artifactId>
                <configuration>
                    <skip>${skipTests}</skip>
                    <failOnFormatViolation>${airness.enforce}</failOnFormatViolation>
                    <failOnNoMatchingProperties>true</failOnNoMatchingProperties>
                    <excludes>
                        <exclude>**/target/**</exclude>
                        <exclude>.git/**</exclude>
                        <exclude>.idea/**</exclude>
                        <exclude>**/*.png</exclude>
                        <exclude>**/*.ttf</exclude>
                        <exclude>**/*.docx</exclude>
                        <!--
                            A picture like the ones above it, but WEBP postdates the binary suffixes the
                            linter passes over of its own accord, so without this it reads the file as
                            text and reports every carriage return in it as a line ending to repair.
                        -->
                        <exclude>**/*.webp</exclude>
                        <!-- Tab-separated data: a tab is the format there, not an indentation choice. -->
                        <exclude>**/*.tsv</exclude>
                        <!--
                            Exported copies of the documentation. The managed .gitignore asset already
                            keeps them out of every consumer's tree, and the exporter writes them without
                            a final newline, so they are excluded by role rather than repaired by hand.
                        -->
                        <exclude>**/README*.html</exclude>
                        <exclude>**/README*.pdf</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>airness-editorconfig-check</id>
                        <phase>package</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!--
                    Fails the build on a dependency whose license the allowlist does not admit, and on
                    one whose license cannot be read at all.
                -->
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>airness-check-dependency-licenses</id>
                        <phase>package</phase>
                        <goals>
                            <goal>add-third-party</goal>
                        </goals>
                        <configuration>
                            <includedScopes>compile,runtime,provided,test</includedScopes>
                            <skipAddThirdParty>${skipTests}</skipAddThirdParty>
                            <failOnBlacklist>${airness.enforce}</failOnBlacklist>
                            <failOnMissing>${airness.enforce}</failOnMissing>
                            <!--
                                The allowlist and the spellings it merges are not written here. They
                                reach this execution from the pluginManagement of the pom this one
                                inherits, which carries the note on what the list admits and why.
                            -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
                OpenRewrite, over a recipe set it discovers rather than one a project configures. The
                recipes live in META-INF/rewrite/ inside airness-config, which OpenRewrite reads off this
                plugin's own classpath, so no project carries a rewrite.yml of its own.

                Declared before the formatter and the import sorter. Under the format profile all three
                apply rather than check, and OpenRewrite's semantic edits have to land before the other
                two re-normalize layout and imports, or the formatter stops being the last word on style.
            -->
            <plugin>
                <groupId>org.openrewrite.maven</groupId>
                <artifactId>rewrite-maven-plugin</artifactId>
                <configuration>
                    <rewriteSkip>${skipTests}</rewriteSkip>
                    <activeRecipes>
                        <recipe>eu.ciechanowiec.airness.Modernization</recipe>
                    </activeRecipes>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>eu.ciechanowiec</groupId>
                        <artifactId>airness-config</artifactId>
                        <version>${airness.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.openrewrite.recipe</groupId>
                        <artifactId>rewrite-apache</artifactId>
                        <version>${rewrite-apache.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.openrewrite.recipe</groupId>
                        <artifactId>rewrite-logging-frameworks</artifactId>
                        <version>${rewrite-logging-frameworks.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.openrewrite.recipe</groupId>
                        <artifactId>rewrite-migrate-java</artifactId>
                        <version>${rewrite-migrate-java.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.openrewrite.recipe</groupId>
                        <artifactId>rewrite-static-analysis</artifactId>
                        <version>${rewrite-static-analysis.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.openrewrite.recipe</groupId>
                        <artifactId>rewrite-testing-frameworks</artifactId>
                        <version>${rewrite-testing-frameworks.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <!--
                        dryRun rather than run. A recipe that would change a source file fails the build
                        and writes the patch it would have applied under target, so a green build is a
                        statement about the tree that was committed rather than about one the build
                        reshaped on its way past.
                    -->
                    <execution>
                        <id>airness-rewrite-apply</id>
                        <phase>${airness.rewrite.apply.phase}</phase>
                        <goals>
                            <goal>runNoFork</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>airness-rewrite-verify</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>dryRunNoFork</goal>
                        </goals>
                        <configuration>
                            <failOnDryRunResults>${airness.enforce}</failOnDryRunResults>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
                The governance checks are goals rather than tests, so -Dtest=OneClass cannot accidentally
                deselect them. The explicit -DskipTests contract is implemented inside every goal and
                bypasses ordinary tests and Airness quality and governance checks. Runtime compatibility
                remains enforced.

                The repository-wide goals run only on the top-level project, so a reactor reports each
                finding once. The per-module goals skip a module with no Java source directory, and
                refuse a module that has one and read nothing out of it.
            -->
            <plugin>
                <groupId>eu.ciechanowiec</groupId>
                <artifactId>airness-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>airness-source-formatting</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>source-formatting</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>airness-preflight</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>tree-snapshot</goal>
                            <goal>check-parameters</goal>
                            <goal>assets-check</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>airness-coverage-evidence</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>coverage-evidence</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>airness-version-check</id>
                        <phase>package</phase>
                        <goals>
                            <goal>dependency-freshness</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>airness-governance</id>
                        <phase>package</phase>
                        <goals>
                            <goal>typography</goal>
                            <goal>entry-files</goal>
                            <goal>comment-prose</goal>
                            <goal>javadoc-links</goal>
                            <goal>test-assertions</goal>
                            <goal>package-cycles</goal>
                            <goal>suppression-budget</goal>
                            <goal>tree-verify</goal>
                        </goals>
                    </execution>
                    <!--
                        The archive is produced in package and finished in package, and those are not the
                        same moment. Maven merges an inherited plugin ahead of one the project declares,
                        so every goal bound here runs before every goal the project binds in the same
                        phase. A project that repackages, through the shade plugin, the assembly plugin,
                        or Spring Boot, would have this goal read the thin archive the jar plugin had
                        just written rather than the archive that ships. Verify is the first phase Maven
                        guarantees is past the whole of package whichever plugin did the repackaging,
                        which is what this check needs and what Airness cannot arrange inside package.
                        The same reasoning already puts publication-content in verify.
                    -->
                    <execution>
                        <id>airness-artifact</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>artifact-content</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                    <!--
                    Supplies the custom com.qulice.checkstyle.* AST checks (paired-brackets notation and
                    the other rules referenced in checkstyle.xml). The qulice plugin jar is a library of
                    check classes on this plugin's isolated classpath, not a plugin this build runs: its
                    goal is never invoked. Its bundled checkstyle is excluded so the plugin runs solely on
                    the pinned ${checkstyle.version} declared above. The standalone qulice-checkstyle
                    artifact is frozen at 0.25.2 and the checks now ship inside qulice-maven-plugin.
                    -->
                    <dependency>
                        <groupId>com.qulice</groupId>
                        <artifactId>qulice-maven-plugin</artifactId>
                        <version>${qulice-maven-plugin.version}</version>
                        <exclusions>
                            <exclusion>
                                <groupId>com.puppycrawl.tools</groupId>
                                <artifactId>checkstyle</artifactId>
                            </exclusion>
                        </exclusions>
                    </dependency>
                    <!--
                        The configuration itself, carried on this plugin's classpath so configLocation
                        below resolves as a resource. This is what replaces the consumer-local file.
                    -->
                    <dependency>
                        <groupId>eu.ciechanowiec</groupId>
                        <artifactId>airness-config</artifactId>
                        <version>${airness.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>eu/ciechanowiec/airness/static_code_analysis/checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <!--
                        failsOnError covers a Checkstyle processing error; failOnViolation covers a rule
                        violation. Both read the switch, or turning it off would leave violations failing
                        the build and the switch would be a half-truth.
                    -->
                    <skip>${skipTests}</skip>
                    <failsOnError>${airness.enforce}</failsOnError>
                    <failOnViolation>${airness.enforce}</failOnViolation>
                    <violationSeverity>info</violationSeverity>
                    <linkXRef>false</linkXRef>
                    <includeResources>false</includeResources>
                    <includeTestResources>false</includeTestResources>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                    <!--
                        This project is the last reactor module and is therefore the first place where
                        the configuration artifact is available while a package build is still running.
                        The ordinary inherited execution sees no Java here because this is a POM
                        project. This non-inherited execution instead checks the implementation modules
                        that produce the harness, without leaking their paths into consumers.
                    -->
                    <execution>
                        <id>airness-self-checkstyle</id>
                        <phase>package</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <inherited>false</inherited>
                        <configuration>
                            <sourceDirectories>
                                <sourceDirectory>${project.basedir}/../airness-annotations/src/main/java</sourceDirectory>
                                <sourceDirectory>${project.basedir}/../airness-governance/src/main/java</sourceDirectory>
                                <sourceDirectory>${project.basedir}/../airness-maven-plugin/src/main/java</sourceDirectory>
                            </sourceDirectories>
                            <testSourceDirectories>
                                <testSourceDirectory>${project.basedir}/../airness-annotations/src/test/java</testSourceDirectory>
                                <testSourceDirectory>${project.basedir}/../airness-governance/src/test/java</testSourceDirectory>
                                <testSourceDirectory>${project.basedir}/../airness-maven-plugin/src/test/java</testSourceDirectory>
                            </testSourceDirectories>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <dependencies>
                    <dependency>
                        <groupId>eu.ciechanowiec</groupId>
                        <artifactId>airness-config</artifactId>
                        <version>${airness.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <rulesets>
                        <ruleset>eu/ciechanowiec/airness/static_code_analysis/pmd.xml</ruleset>
                    </rulesets>
                    <skip>${skipTests}</skip>
                    <failOnViolation>${airness.enforce}</failOnViolation>
                    <printFailingErrors>true</printFailingErrors>
                    <suppressMarker>__AIRNESS_INLINE_SUPPRESSION_DISABLED__</suppressMarker>
                    <includeTests>true</includeTests>
                    <linkXRef>false</linkXRef>
                    <!--
                        The duplication bound. CPD counts tokens rather than lines, so a token
                        count is what the policy has to name: 100 of them is roughly twenty lines
                        of Java. That sits below the fifty-line method ceiling, so a method copied
                        and edited is still caught, and above the incidental repetition that a
                        shared signature or a chained builder produces without anyone copying
                        anything. Literals and identifiers are left significant, so only text that
                        genuinely repeats is reported rather than text that merely rhymes.
                    -->
                    <minimumTokens>100</minimumTokens>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>check</goal>
                            <goal>cpd-check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <dependencies>
                    <dependency>
                        <groupId>eu.ciechanowiec</groupId>
                        <artifactId>airness-config</artifactId>
                        <version>${airness.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <excludeFilterFile>eu/ciechanowiec/airness/static_code_analysis/spotbugs-exclude.xml</excludeFilterFile>
                    <skip>${skipTests}</skip>
                    <failOnError>${airness.enforce}</failOnError>
                    <includeTests>true</includeTests>
                    <effort>Max</effort>
                    <threshold>Low</threshold>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <!-- Findings remain visible, but compilation and tool failures remain fatal. -->
            <id>airness-report-only</id>
            <activation>
                <property>
                    <name>airness.enforce</name>
                    <value>false</value>
                </property>
            </activation>
            <properties>
                <airness.dependency-check.fail-build-on-cvss>11</airness.dependency-check.fail-build-on-cvss>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <testFailureIgnore>true</testFailureIgnore>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <compilerArgs combine.self="override">
                                <arg>-XDcompilePolicy=simple</arg>
                                <arg>--should-stop=ifError=FLOW</arg>
                                <arg>-Xplugin:ErrorProne -XepAllErrorsAsWarnings -Xep:NullAway:WARN -XepOpt:NullAway:AnnotatedPackages=${airness.package.root} -XepOpt:NullAway:ExcludedFieldAnnotations=${airness.nullaway.excluded-field-annotations}</arg>
                                <arg>-Xlint:deprecation</arg>
                                <arg>-Xdoclint:all,-missing</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED</arg>
                                <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>
                                <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED</arg>
                            </compilerArgs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!-- skipTests removes compiler-side analysis as well as lifecycle-bound harness checks. -->
            <id>airness-skip</id>
            <activation>
                <property>
                    <name>skipTests</name>
                    <value>true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <fork>false</fork>
                            <compilerArgs combine.self="override"/>
                            <annotationProcessorPaths combine.self="override">
                                <path>
                                    <groupId>org.projectlombok</groupId>
                                    <artifactId>lombok</artifactId>
                                    <version>${lombok.version}</version>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!--
                A module with no production sources has nothing for the coverage agent to measure.
                Production code without tests is deliberately not skipped: coverage evidence reports it.
            -->
            <id>airness-no-production-code</id>
            <activation>
                <file>
                    <missing>src/main/java</missing>
                </file>
            </activation>
            <properties>
                <airness.coverage.skip>true</airness.coverage.skip>
            </properties>
        </profile>
        <profile>
            <!--
                Applies what the ordinary build only reports. Each source-shaping tool applies its
                changes and is then checked, in that order and in the same phase, so this profile adds
                work rather than removing any: the check still runs and now confirms the apply landed.

                It is opt-in because the alternative is a build that edits the sources it is judging.
                Once that happens, a green result says the tree was acceptable after the build changed
                it, and no one afterwards can tell that from a tree that was acceptable as committed.
            -->
            <id>format</id>
            <properties>
                <airness.source.formatting.apply>true</airness.source.formatting.apply>
                <airness.rewrite.apply.phase>process-resources</airness.rewrite.apply.phase>
            </properties>
        </profile>
        <profile>
            <!--
                The slow half of the harness. What lands here is what reads more than this module: the
                whole commit history, the network, Docker, and the IntelliJ inspection engine. None of
                it belongs in the loop a developer runs a dozen times an
                hour, and all of it runs on every push.

                These are executions in a profile rather than tests carrying a tag. A tag is honoured
                only for as long as nobody overrides the excluded groups, whereas an execution that is
                not in the active profile does not exist to be re-enabled.
            -->
            <id>extended</id>
            <build>
                <plugins>
                    <!--
                        Dependency-Check owns the local database format, while its public data feed is
                        integration-neutral. Every Airness version therefore gets its own database
                        directory and every consumer imports the same daily feed without an NVD API key.
                        A version upgrade cannot accidentally open an incompatible H2 database.

                        The scan includes test dependencies because they execute inside the governed
                        build. Sonatype Guide is disabled because it requires separate credentials; the
                        NVD feed, CISA data, RetireJS data, and hosted suppressions remain enabled.
                    -->
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <configuration>
                            <dataDirectory>
                                ${user.home}/.cache/airness/dependency-check/${dependency-check-maven.version}
                            </dataDirectory>
                            <nvdDatafeedUrl>https://dependency-check.github.io/DependencyCheck_Builder/nvd_cache/nvdcve-{0}.json.gz</nvdDatafeedUrl>
                            <nvdValidForHours>24</nvdValidForHours>
                            <autoUpdate>true</autoUpdate>
                            <failBuildOnCVSS>${airness.dependency-check.fail-build-on-cvss}</failBuildOnCVSS>
                            <failBuildOnUnusedSuppressionRule>true</failBuildOnUnusedSuppressionRule>
                            <skip>${skipTests}</skip>
                            <skipTestScope>false</skipTestScope>
                            <!--
                                The .NET assembly analyzer is off because this harness governs Java and Maven,
                                and because leaving it on makes the verdict depend on the machine. The archive
                                analyzer unpacks every jar, and JNA ships its Windows JNI dispatch libraries as
                                .dll files inside jna.jar, which the Eclipse JDT formatter reaches through
                                org.eclipse.core.filesystem. Those are JNI natives rather than .NET assemblies,
                                but the extension is what routes them, and reading one needs a dotnet runtime.
                                A machine that has one initializes the analyzer and a machine that does not
                                fails to, so the same tree would be scanned two ways: a GitHub runner ships a
                                .NET SDK and an ordinary workstation does not. The determinism the standard
                                gives up for this check covers the public database it reads, not the tooling
                                that happens to be installed beside it.
                            -->
                            <assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
                            <ossIndexAnalyzerEnabled>false</ossIndexAnalyzerEnabled>
                            <versionCheckEnabled>false</versionCheckEnabled>
                        </configuration>
                        <executions>
                            <execution>
                                <id>airness-known-vulnerabilities</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>eu.ciechanowiec</groupId>
                        <artifactId>airness-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <!--
                                    Before anything else in this profile. A truncated clone disarms the
                                    three history goals below at once, and disarms them quietly: the
                                    commits that were never fetched pass by not existing.
                                -->
                                <id>airness-history-preflight</id>
                                <phase>validate</phase>
                                <goals>
                                    <goal>require-full-history</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>airness-governance-extended</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>commit-history</goal>
                                    <goal>commit-typography</goal>
                                    <goal>linear-history</goal>
                                    <goal>scan-secrets</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>airness-qodana</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>qodana</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!--
                Consumers inherit publication checks from this parent rather than from the root reactor.
                Keeping the declaration here avoids making the reactor plugin a build-time dependency of
                the libraries it needs to compile.
            -->
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>eu.ciechanowiec</groupId>
                        <artifactId>airness-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>airness-publication</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>publication-metadata</goal>
                                    <goal>publication-content</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!--
                A project may own one narrowly scoped Dependency-Check suppression document without
                repeating the Airness-owned plugin. The plugin rejects stale rules; the document keeps
                the justification beside each exception in its notes element.

                The document activates this profile, rather than the property naming it. Maven decides
                which profiles are active before it interpolates the properties a pom declares, so a
                property activation reads a project file's setting as absent and would leave the
                document unread on every build that did not repeat it on the command line.
            -->
            <id>airness-dependency-check-suppressions</id>
            <activation>
                <file>
                    <exists>${basedir}/.airness/dependency-check-suppressions.xml</exists>
                </file>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <configuration>
                            <suppressionFiles>
                                <suppressionFile>${airness.dependency-check.suppression.file}</suppressionFile>
                            </suppressionFiles>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
